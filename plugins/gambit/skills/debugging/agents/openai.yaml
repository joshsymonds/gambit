interface:
  display_name: "Debugging — Root Cause Investigation"
  short_description: "Investigates a bug to its root cause with evidence and a failing test, then hands the understandi..."
  default_prompt: "Use $gambit:debugging for this task."
policy:
  allow_implicit_invocation: true
