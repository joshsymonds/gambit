interface:
  display_name: "Task Refinement"
  short_description: "Use this implementation mechanic to refine prepared work briefs only when explicitly invoked by n..."
  default_prompt: "Use $gambit:task-refinement for this task."
policy:
  allow_implicit_invocation: false
