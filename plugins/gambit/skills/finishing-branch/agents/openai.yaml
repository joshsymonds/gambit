interface:
  display_name: "Finishing a Branch"
  short_description: "Integrates a finished branch — merge, pull request, keep, or discard — after verifying tests on t..."
  default_prompt: "Use $gambit:finishing-branch for this task."
policy:
  allow_implicit_invocation: true
