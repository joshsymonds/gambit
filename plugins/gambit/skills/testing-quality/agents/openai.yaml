interface:
  display_name: "Testing Quality Analysis"
  short_description: "Audits an existing test suite for tests that pass without catching bugs, and specifies the replac..."
  default_prompt: "Use $gambit:testing-quality for this task."
policy:
  allow_implicit_invocation: false
