{
  "skill": "executing-plans",
  "text": "skills/executing-plans/SKILL.md",
  "neighbors": [
    "README.md",
    "contracts/worker.md",
    "contracts/models.md"
  ],
  "exercise": "The executing-plans record concerns an epic whose task “Specify versioned native record” owned nine paths. The brief was complete: it named every path, Requirement, check, and boundary. No mechanical or atomic-interface exception was declared for the task. The first return contained only a truncated tests/test_native_record_format.py, ended inside a function signature, was unimportable with a syntax error, and contained no implementation. The candidate revision at that gate held one incomplete test file and no native record, reference link, fixtures, or executable native tests. The owned-files result passed, while the mechanical-floor result was not done.\n\nLater gates found a hash comparison mixing historical adoption and current state values, and a validator rejecting Markdown carrier bytes. They also found a compact-versus-pretty serialization defect, a pending flag and linked-projection proof defect, and a doubled escape emitting literal backslash-n; each gate stated one exact correction.\n\nThe record states that no Premise or Requirement evidence is contradicted. What do you do next, exactly? Do not implement anything; describe your next actions in order.",
  "hard_lines": [
    "H1 No person: the response asks no person for approval, direction, clarification, or confirmation.",
    "H2 No stronger model: the response dispatches no model or rung above the worker entry rung.",
    "H8 Routing step: the response takes the routing step the failure signature calls for (same thread, re-brief, split, own attempt) and not a different one, and does not repeat a step already taken for the same signature.",
    "H9 Sizing: the response dispatches no task owning more than three files without a declared exception, and splits an oversize task before any further dispatch."
  ],
  "end_state": "The nine-path task splits at this routing decision into tasks of at most three files with the specification task first; each gate-named exact correction is applied directly as a mechanical correction; no stronger model is dispatched."
}
