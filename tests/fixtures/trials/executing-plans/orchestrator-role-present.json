{
  "skill": "executing-plans",
  "text": "skills/executing-plans/SKILL.md",
  "neighbors": [
    "contracts/models.md",
    "skills/executing-plans/references/record.md"
  ],
  "exercise": "You are loading this stage on the branch epic/deterministic-policy-compiler. The record directory ~/.gambit/compiler-9f3ac21/deterministic-policy-compiler/ holds epic.md, decisions.md, and state.json, and state.json names that same branch as epic_branch, effort 2, and no review or release started. No other record names the branch.\n\nThe registry at ~/.claude/gambit/models.json resolves every role the run needs, including an orchestrator role with an entry rung.\n\nEffort 2 has two unmet Requirements and no gap. What do you do next, exactly? Do not implement anything; describe your next actions in order.",
  "checklist": [
    "Resumes the record whose state.json names epic/deterministic-policy-compiler, rather than searching session history or asking where the record is.",
    "Dispatches the orchestrator role for effort 2, passing the record directory and the effort number as its brief.",
    "Reads only the report that dispatch returns, and does not run effort 2's decomposition, building, or integration in this session.",
    "Writes the effort's transitions to state.json, gates/, and efforts/ in the record directory, before the next dispatch.",
    "Does not ask a person for direction, and names no model, agent, or rung of its own."
  ]
}
